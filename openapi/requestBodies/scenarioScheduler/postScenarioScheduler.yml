description: Request body for creating a scenario scheduler.
content:
  application/json:
    schema:
      $ref: ../../schemas/scenarioScheduler/scenarioSchedulerRequestBody.yml
    examples:
      scenario scheduler example:
        $ref: ../../examples/scenarioScheduler/scenarioSchedulerRequest.yml
